# Robodog GitHub Pages

將此資料夾內的所有檔案上傳到 GitHub repository 根目錄。

網站入口：

- `index.html`：已內嵌並壓縮網格與材質，可直接顯示，不依賴額外網格資料檔

模型下載檔案：

- `quadruped_textured.glb`
- `quadruped_colored.stp`
- `quadruped_initial.stp`

所有檔案均低於 25 MB。GitHub Pages 設定為 `main` branch 的根目錄後，等待部署完成再重新整理頁面。
